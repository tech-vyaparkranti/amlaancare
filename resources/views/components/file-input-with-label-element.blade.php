<div class="form-group">
    <label for="{{ $id }}">{{ $label }}</label>
    <input type="file" 
           name="{{ $name }}" 
           id="{{ $id }}" 
           class="form-control" 
           {{-- placeholder="{{ $placeholder }}" --}}
           >
</div>
